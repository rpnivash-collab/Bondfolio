
  # Design as per PDF

  This is a code bundle for Design as per PDF. The original project is available at https://www.figma.com/design/QFCTsdNUvkGGxwKfQqIXDE/Design-as-per-PDF.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  